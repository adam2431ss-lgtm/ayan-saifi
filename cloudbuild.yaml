steps:
- name: 'gcr.io/cloud-builders/docker'
  args: [
    'build',
    '-t', 'gcr.io/$PROJECT_ID/google-map-scraper:$COMMIT_SHA',
    './Working-Google-Map-Scraper'
    ]
images:
- 'gcr.io/$PROJECT_ID/google-map-scraper:$COMMIT_SHA'
